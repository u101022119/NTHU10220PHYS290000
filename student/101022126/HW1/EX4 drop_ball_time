import math
h = float(raw_input("height (m) >> "))

if h<0 :
    print "The height must be zero or positive number."
    
else:
    t = math.sqrt(2 * h / 9.8)
    v = 9.8 * t
    
    print "After", t,"seconds, the ball above the ground ", h, "meters will hit the ground at a speed", v, "m/s."
