import math
fruit='pineapple'

#'''
index = -1
while index > -1-len(fruit):
    letter = fruit[index]
    print letter
    index = index - 1
'''
'''
def find(word, letter, start):
    index = star
    while index < len(word):
        if word[index] == letter:
            return index
        index = index + 1
    return -1
'''
'''
def count(word,letter):
    count=0           #count is not allow to locate outside the function
    for Letter in word:
        if Letter == letter:
            count = count + 1
    print count
'''
'''
def count(word,letter,start):
    count=0           #count is not allow to locate outside the function
    index=start
    while index<len(word):
        if word[index] == letter:
                count = count + 1
        index +=1
    print count
'''
#'''
